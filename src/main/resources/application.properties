# Database connection settings
spring.datasource.url=jdbc:mysql://localhost:3306/eda_auth
spring.datasource.username=eda_users
spring.datasource.password=edapassword

# Specify the DBMS
spring.jpa.database = MYSQL

# Show or not log for each sql query
spring.jpa.show-sql = false

# Hibernate ddl auto (create , create-drop, update )
spring.jpa.hibernate.ddl-auto = update

# Use spring.jpa.properties.* for hibernate native properties (the prefix is
# stripped before adding them to the entity manager )
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQL8Dialect


spring.mail.host=smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=7adc9d9010893f
spring.mail.password=f6a9e21edb7f94



